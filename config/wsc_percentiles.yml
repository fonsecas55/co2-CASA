# Global SIMI bounds for WSC normalisation (Wu 2022, eq. 3-5).
#
# PLACEHOLDER VALUES — TODO: calibrate as the 2nd/98th percentiles of SIMI over a
# large historical sample. SIMI uses [0,1] reflectance, so its theoretical range
# is [0, 0.7071*sqrt(2)] ~= [0, 1.0]. Conservative full-range placeholder below.
simi_min: 0.0
simi_max: 1.0
